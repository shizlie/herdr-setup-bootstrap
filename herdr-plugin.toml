id = "setup-bootstrap"
name = "Setup Bootstrap"
version = "0.1.0"
description = "Run ./setup.sh in every newly created worktree checkout if the repo provides one."
min_herdr_version = "0.7.0"

platforms = ["linux", "macos"]

[[events]]
on = "worktree.created"
command = ["./bootstrap.sh"]
